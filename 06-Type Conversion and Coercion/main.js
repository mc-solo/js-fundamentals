// explicit conversions

const x = "7";
const y = 40;
const sum = Number(x) + y;

console.log(
  `Type of x: ${typeof x}, Type of Number(x): ${typeof Number(x)}, sum: ${sum}`
);

// implicit conversion

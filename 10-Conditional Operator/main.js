// condition ? expression_if_true : expression_if_false;

const age = 47;

age >= 40
  ? console.log("Too old to live on this fucking earth")
  : console.log("remember we all are gonna die one day");

let number;
let isEven = number % 2 === 0 ? true : false;
